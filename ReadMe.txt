-Edit Configuration in  Configuration/PublishBot.Config.cmd

-Must Store password in publish profile. 

-Uses Conveyor for IIS/Playfab testing. 

-RESTApi need local link (add as a link) to database.

-test